
-- 超簡單語音輸入
require("hs.ipc")   -- 讓 `hs` 命令列工具能連進來(除錯/檢查權限用)
dofile(os.getenv("HOME") .. "/.hammerspoon/voice-input.lua")

-- 超簡單語音輸入（選單列）
require("voice-input-menubar")

-- 超簡單語音輸入（提示音，本地擴充）
require("voice-input-chime")

-- 超簡單語音輸入（修正 core.run 凍結事件迴圈，本地擴充）
require("voice-input-run-fix")

-- 超簡單語音輸入（⌃⌘V 開關面板，本地擴充）
require("voice-input-panel-hotkey")
